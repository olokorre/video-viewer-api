{
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "clean": "rm -rf dist/*/",
    "main": "tsc && node ./dist/main",
    "dev": "ts-node-dev --respawn --transpile-only -- source/main.ts"
  }
}
